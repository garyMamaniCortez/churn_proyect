loguru
mkdocs
pip
pytest
pytest-mock
python-dotenv
ruff
tqdm
typer
-e .

# Data extraction / versioning
sqlalchemy>=2.0
psycopg2-binary
pandas
pydantic>=2.0
pydantic-settings
dvc

# EDA / churn modeling
matplotlib
seaborn
scikit-learn
mlflow
tensorflow
# ^ if you're on a machine with no GPU and want a smaller install,
#   `tensorflow-cpu` is a drop-in replacement.

# Added in a later phase, once we build the serving API:
# fastapi, uvicorn                     -> model serving

